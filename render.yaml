services:
  - type: web
    name: digital-behaviour-twin
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend app:app --bind 0.0.0.0:$PORT
    autoDeploy: true
